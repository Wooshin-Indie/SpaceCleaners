using MPGame.UI.GameScene;
using UnityEngine;

public class RadarHUD : PlayerHUD
{
    private GameObject radarIcon; // 원 모양 아이콘
    private bool isRadarActive = false;

    private void OnUpdateRadar()
    {
        if (isRadarActive)
        {
        }
    }
}
